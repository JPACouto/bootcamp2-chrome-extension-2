version: "3.8"
services:
  e2e:
    build: .
    image: jpacouto/ext-e2e:latest
    environment:
      - CI=true
    volumes:
      - ./:/app
    working_dir: /app
    command: npm run test:e2e
    shm_size: 2gb  # evita falhas do Chromium por falta de shared memory

